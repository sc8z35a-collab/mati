# 許可後の検証計画

**現在は未実行。ユーザーは実装の終盤に許可を求めてからテストするよう指定しています。**

## 1. 静的確認とシミュレーション

- JSの構文、HTML参照、差分を確認。
- `node tests/traffic-regressions.cjs`: 既存の停止・横断・衝突、8方向の旋回、交差点予約、境界位置同期、異なるフレーム刻みでの交通時計。
- フォーマットはコード編集の一部として適用済み。ゲームの実行結果ではありません。

## 2. ブラウザ回帰

HTTPサーバー、WebGLのあるブラウザ、Playwrightが必要です。`TEST_URL`で対象サーバー、`PLAYWRIGHT_MODULE`でインストール先を指定できます。CDNが利用できない検証環境では`THREE_SCRIPT`で**同じr158**のスクリプトを指定できます。

| スクリプト                      | 対象                                                                      |
| ------------------------------- | ------------------------------------------------------------------------- |
| `tests/objects.cjs`             | 最新版で追加済みの小物、通路、住宅サイズ、住戸設備、ロード／解放          |
| `tests/traffic-regressions.cjs` | 描画を使わない交通の回帰                                                  |
| `tests/hdr-ultra.cjs`           | 軽量4KとHDR 4K実出力、PNG寸法、遮蔽物、縦横比、キャンセル、アルバム再読込 |
| `tests/gameplay.cjs`            | 通常起動の保存復元、地図、依頼、実ループ、バルコニーの歩行、モバイル表示  |

4Kテストは既定で省略しません。HDRが利用できないGPUでは**検証不能**として終了し、合格扱いにしません。従来のオブジェクト検査には手動フレーム制御がありますが、その結果をFPSの証拠として使いません。

性能テストは実ループのp50 / p95 / p99を記録します。必要なら`MAX_P95_MS`で予算を指定します。機種も条件も未確定の状態で任意のFPS合格値を設定しません。

## 3. 画像・操作の確認

`.artifacts/`に出力された画像を実際に開いて確認します。

- 昼・夕・夜・雨、公園・道路・住宅・屋上。
- 同じ視点・同じ表示サイズで元版と比較。草木、花、ファサード、光源切替、影のちらつき。
- 4K原寸のタイル境界、葉の輪郭、細い窓枠、近距離の材質。
- PCと390×844の縦長画面。カメラの設定が視点・シャッターを塞がないか。
- 操作ラベル、通知、室内の依頼追跡、バルコニー扉。

## 4. 異常系と継続プレイ

- WebGL中断、アルバム容量不足、IndexedDB更新を古いタブが阻む場合。
- 旧v1アルバム移行、旧v3セーブ、位置記録の破損、再読み込み直後の自動保存。
- 連続撮影・取消し・フロア移動の後、テクスチャ／形状数が増え続けないか。
- 交通が複数周期を過ぎても進行できるか。

問題が出たら修正し、関連範囲を再確認します。テストが通ったことと、見た目を確認したことは別に報告します。

## 公開

この工程には本番へのpush・マージ・デプロイを含めません。検証結果と修正差分を提示してから公開の扱いを決めます。
